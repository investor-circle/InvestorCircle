# Base URL of the main app's public API. Read-only, unauthenticated,
# already-public data (see api/_lib/handlers/public-ideas.js in the main
# repo) — safe to point at production from local dev.
PUBLIC_API_BASE=https://myinvestorcircle.com
