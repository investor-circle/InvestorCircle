# ── Finnhub (live prices) ──────────────────────────────────────────────────────
# Sign up free at https://finnhub.io — takes 30 seconds, no credit card.
# Copy your API key from the Finnhub dashboard and paste it below.
# Free tier: 60 requests/minute. More than enough for a personal portfolio.
VITE_FINNHUB_KEY=d930ev1r01qpou38odagd930ev1r01qpou38odb0

# ── Neon (database) ───────────────────────────────────────────────────────────
# Find this in your Neon dashboard: Project → Connection Details → Connection string.
# Only needed when you wire up real persistence; the app runs on mock data without it.
VITE_DATABASE_URL=postgresql://user:password@ep-xxx-xxxxxx.ap-south-1.aws.neon.tech/neondb?sslmode=require

# ⚠️  Keep .env out of git — it's already in .gitignore.
# ⚠️  For production: move API calls to a server-side function so keys
#     never reach the browser bundle. Fine for a personal prototype.
